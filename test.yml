---
- name: launchin complete apache website
  hosts: localhost
  become: true
  tasks:
          - name: installing apache
            apt:
                    name: telnet
                    state: present
